alias zshhelp='cat ~/.oh-my-zsh/custom/help-prompt.zsh'
# C-w kill word backward
# M-d kill word forward

# M-b move word backward
# M-f move word forward

# C-k kill line forward
# C-u kill line backward

# E-Q puts buffer on stack. 
#	(Allows you to run other command. Once that command is completed, the stack will pop, and you can continue the previous command.)

# E-A run command w/o clearing buffer
#	(cursor placement is retained.)

# E-' "quote" and \escape buffer
